<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/material_sub_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/material_sub_bg" />
    <corners android:radius="6dp" />
</shape>
